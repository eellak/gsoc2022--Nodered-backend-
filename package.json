{
    "scripts": {
        "start":"cd api/ && npm install && npm run server",
        "heroku-prebuild":"cd client/ && npm install",
        "heroku-postbuild":"cd client/ && npm run build && npm start"
    }
}